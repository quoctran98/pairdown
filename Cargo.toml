[package]
name = "pairdown"
version = "0.1.0"
authors = ["Quoc Tran"]
edition = "2024"
description = "A tool to downsample (pare down) paired end FASTQs, because I couldn't find one for some reason, though I'm sure one exists."

[dependencies]
clap = { version = "4.5.51", features = ["derive"] }
rand = "0.9.2"
rand_chacha = "0.9.0"
